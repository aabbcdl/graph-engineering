# Fixture Instructions

Treat `README.md` as the product and security contract. Review the complete
workspace, implement only evidence-backed fixes, and preserve public APIs.

Run `npm test` after changes. Do not commit, push, deploy, access external
services, or read files outside this workspace.
