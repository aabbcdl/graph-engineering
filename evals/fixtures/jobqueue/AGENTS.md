# Fixture Instructions

Treat `README.md` and `docs/contract.md` as the public product and reliability
contract. Review the complete workspace, implement only evidence-backed fixes,
and preserve exported APIs.

Run `go build ./...` and `go test ./...` after changes. Do not commit, push,
deploy, access external services, or read files outside this workspace.
