interface:
  display_name: "Graph Engineering"
  short_description: "Run an explicitly approved repository engineering graph"
  default_prompt: "Use $autonomous-engineering-graph for this explicitly approved task, keep its read-only live progress attached, and report the evidence-backed terminal result."

policy:
  allow_implicit_invocation: false
